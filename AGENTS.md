# Templo Virtual (sgc-dm)

Notas para agentes de código neste repositório:

- Prefira mudanças pequenas e focadas; não reescreva documentação não pedida.
- Schema do banco: sempre via migrations em `supabase/migrations/`.
- Deploy do app: Cloudflare Workers (Nitro). Não versionar secrets.
